Use when the page already has a pill segmented control, or when the sections are peers rather than filters.

```jsx
<UnderlineTabs value={t} onChange={setT} tabs={[{value:'d',label:'Mes demandes'},{value:'p',label:'Mes trajets publiés'}]} />
```
