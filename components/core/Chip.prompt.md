Used for the route list ("Paris ↔ Alger"), the allowed/forbidden object lists, and filters.

```jsx
<Chip icon={<Icon name="plane" size={14} />}>Paris ↔ Alger</Chip>
<Chip forbidden icon={<Icon name="ban" size={14} />}>Argent liquide</Chip>
```
