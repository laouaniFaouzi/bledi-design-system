Le code unique est le geste central de Bledi : c'est lui qui libère le paiement.

```jsx
<CodeInput value={code} onChange={setCode} />
```

Six caractères par défaut, avance automatique, retour arrière sur case vide.
