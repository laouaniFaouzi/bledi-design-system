Pour un réglage qui s'applique immédiatement. Pour un engagement à valider, utilisez `Checkbox`.

```jsx
<Switch checked={notif} onChange={setNotif}
  label="Alertes de nouvelles demandes" hint="Sur vos trajets déclarés uniquement." />
```
